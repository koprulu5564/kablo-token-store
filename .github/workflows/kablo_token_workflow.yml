name: Refresh Token Daily

on:
  schedule:
    - cron: '0 2 * * *'  # UTC 02:00 (örneğin Türkiye saati ~05:00)
  workflow_dispatch:

jobs:
  update-token:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: python get_token.py
      - run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add stream_token.txt || true
          git commit -m "🔄 Token güncellendi" || echo "No changes"
          git push || echo "Push edilmedi"
